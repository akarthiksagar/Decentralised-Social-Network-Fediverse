VITE_API_URL=https://api.example.com
VITE_SERVER_NAME=Example Social
VITE_SERVER_DOMAIN=example.com

# Optional bootstrap list. The app also loads live servers from the backend /instances directory.
# Use real deployed backend API URLs only. Keep this as valid JSON on one line in deployment dashboards.
VITE_SERVER_DIRECTORY=[{"name":"Example Social","domain":"example.com","apiUrl":"https://api.example.com","category":"General","registrations":"Open"}]
